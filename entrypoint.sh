#!/bin/sh -l

# load configuration file

# decode all base64 input files from the authentication input

# get cluster status command

# create cluster command

# get kube credentials command

echo "Hello $1"
time=$(date)
echo ::set-output name=time::$time
